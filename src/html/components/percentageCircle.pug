mixin percentageCircle(percent)
  svg.percentageCircle(viewBox="0 0 100 100" preserveAspectRatio="none")
    circle(cx="50" cy="50" r="40").circle.circle__under
    circle(cx="50" cy="50" r="40" style=`animation-delay: -${percent}s`).circle.circle__above